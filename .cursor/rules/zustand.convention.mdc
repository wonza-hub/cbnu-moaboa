---
alwaysApply: true
---

# Zustand 규칙

## 스토어

### 미들웨어

- `devtools` + `persist` + `subscribeWithSelector` + `immer` + `combine` 순으로 감싼다.
- TypeScript 사용 시, `combine`은 필수로 사용해야 한다.

## 셀렉터

- 셀렉터의 경우 커스텀 훅으로 정의하여 `export` 해야 한다. 사용하는 곳에서는 특정 커스텀 훅을 `import` 하여 사용할 수 있다.
